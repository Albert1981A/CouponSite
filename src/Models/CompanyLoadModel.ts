class CompanyLoadModel {
    public id ? : number;
    public name ? : string;
    public email ? : string;
    public password ? : string;
    public imageID ? : string;
    public image ? : FileList;
}

export default CompanyLoadModel;
